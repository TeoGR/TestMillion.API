# ========= TestMillion.API - .env.template =========
# Copia este archivo a `.env` en la raíz del repo:
#   cp .env.template .env
# y ajusta valores si lo deseas.

# Entorno ASP.NET Core (Development | Staging | Production)
ASPNETCORE_ENVIRONMENT=Development

# Contraseña del usuario 'sa' de SQL Server (contenedor db)
# Cambia este valor si lo prefieres.
SA_PASSWORD=Your_password<123

# En Docker solo exponemos HTTP:8080, por eso desactivamos la redirección a HTTPS.
# Si configuras HTTPS dentro del contenedor, cambia a false o elimina esta variable.
DISABLE_HTTPS_REDIRECT=true

# NOTA IMPORTANTE:
# Las credenciales de Firebase NO van en este archivo (.env).
# Debes colocar el Service Account JSON en ./secrets/firebase_credentials.json
# (Docker lo montará como secret en /run/secrets/firebase_credentials)
